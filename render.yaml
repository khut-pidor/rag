services:
  - type: web
    name: rag-cota
    env: python
    pythonVersion: "3.12.7"  # ← FORCES WORKING PYTHON
    buildCommand: "pip install poetry && poetry install --no-interaction"
    startCommand: "poetry run gunicorn app:app"
    envVars:
      - key: GEMINI_API_KEY
        sync: false